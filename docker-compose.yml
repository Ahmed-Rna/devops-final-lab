services:
  # MongoDB Database Service (Commented out - using MongoDB Atlas instead)
  # mongodb:
  #   image: mongo:7.0
  #   container_name: medicare-mongodb
  #   restart: unless-stopped
  #   ports:
  #     - "27017:27017"
  #   environment:
  #     MONGO_INITDB_DATABASE: medicine-store
  #   volumes:
  #     - mongodb_data:/data/db
  #   networks:
  #     - medicare-network
  #   healthcheck:
  #     test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/medicine-store --quiet
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Backend API Service
  backend:
    build:
      context: ./FinalLab-backend
      dockerfile: Dockerfile
    container_name: medicare-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      MONGODB_URI: mongodb+srv://aw2578527_db_user:NEKWSEQb159AtyzI@cluster0.wswweds.mongodb.net/medicine-store?retryWrites=true&w=majority
      FRONTEND_URL: http://localhost:5173
    networks:
      - medicare-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend Service
  frontend:
    build:
      context: ./FinalLab-frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: http://localhost:3000
    container_name: medicare-frontend
    restart: unless-stopped
    ports:
      - "5173:80"
    depends_on:
      - backend
    networks:
      - medicare-network

networks:
  medicare-network:
    driver: bridge
